// 注释
let x;

x = 3 + y;
console.log(x);

foo(x, y);
obj.bar(3);

if (x === 0) {
    x = 123;
}

/**
 * @param  {string} a a
 * @param  {string} b b
 * @return {string} a+b
 */
function foo(a, b) {
    return a + b;
}
